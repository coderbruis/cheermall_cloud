spring:
  application:
    name: mall-gateway
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8001
        group: DEFAULT_GROUP
        prefix: mall-gateway
        file-extension: yaml
      discovery:
        server-addr: 127.0.0.1:8001