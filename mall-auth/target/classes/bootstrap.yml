spring:
  application:
    name: MALL-Auth
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8001
        group: DEFAULT_GROUP
        prefix: mall-auth
        file-extension: yaml
      discovery:
        server-addr: 127.0.0.1:8001